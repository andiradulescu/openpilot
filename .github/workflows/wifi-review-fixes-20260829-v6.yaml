name: apply wifi review fixes v6
on:
  push:
    branches: [wifi-review-fixes-20260829]

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7
      with:
        ref: wifi-no-networkmanager-refactor
        fetch-depth: 0

    - name: Apply fixes
      shell: bash
      run: |
        set -euxo pipefail
        git show origin/wifi-review-fixes-20260829:.tmp/run_wifi_review_fixes_20260829_v6.py > /tmp/run_wifi_review_fixes_v6.py
        python3 /tmp/run_wifi_review_fixes_v6.py
